[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

markers =
    smoke: Smoke tests for critical paths (fast, minimal assertions)
    e2e: End-to-end tests
    feature: Feature-level tests
    slow: Tests that take longer to run
